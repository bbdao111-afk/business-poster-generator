---
name: business-poster-generator
description: 按「素材收集 → 方案选版 → 成品输出」三步门禁流程，从产品彩页、方案 PDF/Word 或客户案例介绍中提炼商业卖点，生成风格严格区分的产品宣传海报或客户案例海报。强制先收集必选素材、先让用户从多个大标题/背景/版式方案中选择，拿到确认后才输出成品。支持中文展示字体选版、复杂字清晰度保护、300 DPI 印刷级导出。适用于中文 B2B 科技、云计算、IT 基础设施与企业服务营销；不用于普通活动海报或纯艺术海报。
agent_created: true
---

# 商业海报生成器

把输入材料先转化为可核验的结构化内容，再生成可编辑 HTML 和高分辨率 PNG；需要印刷时再导出 PDF。关键中文、数字、图表、Logo 和二维码必须由排版层渲染，不交给图片模型生成。

完整输入清单、请求模板和工具命令见 [使用说明](使用说明.md)。**完整流程细节与门禁话术见 [生成流程（三步门禁）](references/workflow.md)。**

## 工作流：三步门禁（必须照做）

本 Skill **不是"一次给结果"的工具**，而是逐步引导的流程。三个硬门禁，未拿到用户确认不得跨越：

### 第一步 · 素材收集与核验 → 🚦 门禁一

- **先输出素材清单，不要先动手做海报**。按产品海报 / 案例海报给出必选与可选项表格。
- 逐项核验：Logo 是否需要裁透明留白、图片尺寸、数据口径、原文事实与营销改写的边界、内部信息剔除。
- 必选项齐全并得到用户放行后，才能进第二步；缺项时只输出缺失清单并停下。

### 第二步 · 方案选版（强制等待选择）→ 🚦 门禁二

必须同时给出**可编号选择**的三类候选，不能只描述一种方案：

1. **大标题文案候选 3–5 条**（一张海报一个主结论，`{}` 标记分色关键词）。
2. **字体 × 效果 × 配色**：跑选版页 / 方案册，让用户看着图选。
   ```bash
   python3 art-type/scripts/make_select_page.py "标题{关键词}" --output <目录> --default-fx block-tech
   python3 scripts/make_options.py --output <目录> --theme light \
     --option "文案A|sans|block-tech" --option "文案B|pangmen|dual-tone"
   ```
3. **版式与背景方向**：产品版浅底 / 案例版深底、指标区形态、是否放套餐表与界面实拍。

每个选项要**编号 + 渲染预览 + 一句话说明 + 可复制的 brief 字段**。
**未收到明确选择（编号或指定组合）前，只重出选项，绝不渲染成品海报。**

### 第三步 · 成品输出

仅在通过门禁二后执行：

```bash
python3 scripts/render_poster.py <brief.json> --output <poster.html>
python3 scripts/export_png.py <poster.html> --output <poster@300.png> --dpi 300
```

出图后按下节「最低质检」逐条核对，交付 `brief.json` + `poster.html` + `poster@300.png`，
并列出所有未被原文直接支持的营销改写。

### 修改轮次怎么回退

- 改文案 / 换截图 → 原地改完重出
- 改字体 / 改效果 / 改版式 → **回到第二步**重出选项再等确认
- 改主题 / 换客户 / 换素材 → 回到第一步

## 路由

- 推进三步门禁流程、需要门禁话术与质检清单：读取 [生成流程（三步门禁）](references/workflow.md)。
- 输入是产品彩页、方案书、参数表、产品介绍：读取 [产品海报规范](references/product-poster.md)。
- 输入是客户案例、项目介绍、成功故事：读取 [客户案例规范](references/case-poster.md)。
- 需要准备渲染数据或调用模板时：读取 [输入与输出规范](references/input-output.md)。
- 处理海报大标题的字体、字距、效果或选版时：读取 [艺术字规则层](references/art-type.md)。
- 对照设计师版优化科技背景、标题光效、切角卡片或动态光轨时：读取 [科技海报特效层](references/tech-poster-fx.md)。
- **要把本 Skill、示例、模板、截图或字体包发布到公开仓库 / 交付第三方 / 发到社群时：强制先读取 [零指向脱敏规范](references/desensitize.md)，并跑 `python3 scripts/check_pointing_info.py --terms <你的禁词表>`；error 未清零一律停下，不得发布。**

### 两种风格自动判定（进入门禁一时先做，并把结论说出来）

本 Skill 有两种输出风格，**用途决定版式**，不能等用户声明：

| 风格 | 画布 | 主题 | 靠什么建立可信度 | 信号权重 |
|---|---|---|---|---|
| 产品宣传版 | 794×1123 | 浅底 | 结构与证据：指标卡、界面实拍、核心能力、套餐表 | 彩页/方案书/参数表 +3；界面截图、套餐价格、"产品能做什么" 各 +2 |
| 客户案例版 | 1080×1440 | 深底 | 场景与背书：联合品牌、方案卡、实景 hero、客户故事 | 客户名称与 Logo、场景/问题/成果 各 +3；实景照片、"客户发生了什么变化" +2 |

判定顺序，必须照做：

1. **用户已明确指定风格 → 直接采用**，不再自行判断。
2. 未指定 → 按上表打分，**分高者胜**；在门禁一的素材清单顶部**公开写出判定结论与依据**（例："判定：客户案例版（深底）· 依据：有客户 Logo 与落地成果"），并允许用户一句话推翻。
3. **分差 ≤1 视为混合内容 → 禁止自行选择**，必须把两种版式都做成编号选项让用户挑。
4. **零信号 → 停下**，先要素材，不得凭猜测开做。

风格选定后连带决定：主题浅/深底、画布尺寸、可用效果集合（浅底禁用深色版 `tech-shadow`，会与背景同色）、字体与效果推荐。

## 内容处理底线（各阶段通用）

- 读取全部输入，标记“原文事实、可计算事实、营销改写、缺失项”。不得补造客户名称、百分比、排名、型号、认证、部署规模或效果数据。
- 先产出一份精简内容稿，再进入视觉排版。内容超量时按“核心价值 > 量化证据 > 关键能力 > 规格细节”裁剪，不通过缩小字号硬塞。
- 从材料中提取品牌 Logo、产品图、客户实景、图表和二维码。低清或缺失素材用明确占位，不伪造客户 Logo 或二维码。
- **文案口径与原始材料逐字核对**（例如授权对象是“AI 助手授权”不是“AI 桌面云授权”）。
- 内部材料里的接口人姓名、内部系统路径、发文编号、“仅限内部公开”标记一律不进海报。
- **真实交付与对外公开严格分开**：交付给客户/渠道/印刷的海报必须使用真实名称与口径，不得脱敏（替换名称＝造假）；而对外公开的 Skill、示例 brief、截图、模板必须零指向（无厂商/产品/客户/内部信息），发布前跑 `scripts/check_pointing_info.py`，并按 [零指向脱敏规范](references/desensitize.md) 替换图片、Logo、文件名与 Git 历史。

## 视觉原则

- 品牌一致性优先。从用户模板提取颜色、Logo、安全边距和字体倾向；大标题默认使用打包的展示字体（首选优设标题黑，机械切角、锐利无圆角，贴合中文 B2B 科技海报常见风格），缺字或用户指定时回退 `PingFang SC, Microsoft YaHei, sans-serif`。展示字体通过 `@font-face` 打包进输出，不依赖系统字体。
- 全图最多两种字体、三档主要字号、一个主强调色。正文不得低于最终 1080px 宽画布中的 24px。
- 大字排版规则：按字体分配置字距，超宽超重字体（优设标题黑、庞门正道标题体）用 0 至 +0.02em 正字距，防止「核心数据」等复杂字形笔画粘连；常规黑体用 -0.02em 负字距；行高 0.95–1.1；标题尽量 8 字以内；需要斜体感时用 CSS `skewX` 补充（0.02–0.18），不用伪斜体。
- 清晰度硬规则：默认纯净大字关闭 text-shadow、filter、伪斜体和额外描边；案例模板原有的标题发光必须被 `.h1art` 覆盖。复杂中文不叠加强描边和强发光；优先保证「核、数、据、管、理、安、全」等多部件字的内部留白。
- 默认组合：优设标题黑 × 纯净大字。效果库包含 11 种：纯净大字、渐变填充、双色分阶压印、双层描边、空心描边混排、斜切渐变块反白、立体挤出、科技主题阴影（暗底）、科技主题阴影（浅底）、霓虹发光（暗底）、斜切冲击。案例对照设计师版时，优先把 `tech-shadow` 作为大主题效果候选，与字体分别组合选版；产品宣传海报为浅底版式，优先从浅底效果里挑：**`block-tech`（斜切渐变块反白，清晰度与焦点最强）、`dual-tone`（双色分阶压印）、`tech-shadow-light`**。白底误用深色版 `tech-shadow` 会导致浅色标题与背景同色。
- 产品海报靠结构和证据建立可信度；案例海报靠场景、客户背书与结果叙事建立可信度。
- AI 生成图只用于无文字背景、抽象科技元素或不代表真实项目的装饰图。真实客户案例优先用用户提供的现场照片。

## 最低质检

第三步出图后逐条核对：

- 输入中的关键数字逐项核对；单位、正负号、百分号与比较基准完整。
- 文案口径与原始材料逐字一致。
- 多笔画标题字（服、刺、套、基、座）一眼可辨，无糊字、无重影。
- 4 张界面截图比例一致、绿色标签齐平；文字与卡片不溢出，底部 CTA 条完整（取底部像素确认不是白边）。
- Logo 不变形、不改色、未被裁切；二维码实际可识别并保留四周静区。
- 上市/退市时间、价格声明等免责信息齐全。
- 标题在 3 秒内可读懂；单张海报只表达一个主结论。
- 输出文件名包含类型与主题，例如 `product-<主题>-poster.html` 或 `case-客户名-poster.png`；文件名本身也是指向信息，对外发布前一并检查。
- **本次产物若要对外公开**（上传仓库、发社群、给客户以外的人）：另跑 `python3 scripts/check_pointing_info.py --terms <禁词表> <要发布的路径>`，error 清零才算交付。
