# 商业海报生成器使用说明

## 一、Skill 能做什么

本 Skill 用于生成中文 B2B 科技、云计算、桌面云、IT 基础设施和企业服务类海报，支持两类输出：

- **客户案例海报**：客户 Logo + 公司 Logo + 客户场景背景图 + 案例介绍 + 案例大标题。
- **产品宣传海报**：公司 Logo + 产品/方案素材 + 产品卖点 + 参数和价值数据。

输出可以是可编辑 HTML，也可以进一步导出 PNG；需要印刷时再导出 PDF。

核心原则：事实内容、中文标题、数字、Logo、二维码全部由排版层渲染，不交给图片生成模型绘制。

---

## 二、生成客户案例海报需要提供什么

### 必需内容

产品宣传版默认将公司 Logo 放在海报左上角；案例版将公司 Logo 放在联合品牌区。

| 内容 | 字段/形式 | 要求 |
|---|---|---|
| 客户名称 | 文字 | 提供正式简称，例如“示例制造”；全称可一并提供 |
| 客户 Logo | PNG/JPG/WebP | 透明底 PNG 优先；提供清晰原图，不要截图压缩版 |
| 公司 Logo | PNG/SVG/WebP | 指提供方案/产品公司的 Logo，例如示例科技 Logo；透明底优先 |
| 案例介绍 | Word/PDF/文字 | 至少包含客户场景、遇到的问题、采用的方案、产生的变化；必须说明哪些内容可公开 |
| 案例背景图片 | JPG/PNG/WebP | 客户厂区、楼宇、机房、园区或业务场景；优先使用客户提供的真实图片 |

### 可选内容

| 内容 | 字段/形式 | 说明 |
|---|---|---|
| 海报大标题 | 一段文字 | 可直接提供；不提供时由 Skill 根据案例事实自动总结，并标记为营销改写 |
| 标题高亮词 | `{关键词}` | 例如 `护航{AI算力产业链}拔节生长`，高亮词会使用品牌绿和独立阴影 |
| 二维码 | PNG/JPG | 提供真实二维码；不提供时只保留二维码占位，不生成假码 |
| 方案名称 | 文字 | 例如“桌面云”“零信任”“数据安全” |
| 量化成果 | 文字/数据 | 必须提供原始口径和比较基准，不接受无来源的百分比或排名 |
| 品牌色/模板 | 图片、色值或旧海报 | 用于保持系列海报一致性 |
| 产品截图 | PNG/JPG/WebP | 可提供 AI 对话、诊断、修复、看板等界面截图；会作为功能证据嵌入产品海报 |
| 输出要求 | 文字 | PNG、HTML、PDF、尺寸、印刷 DPI、是否需要出血等 |

### 产品宣传海报需要提供什么

| 内容 | 字段/形式 | 要求 |
|---|---|---|
| 公司 Logo | `company_logo` | 必选，固定放左上角；透明底 PNG 优先 |
| 产品名称与一句话卖点 | `title` / `subtitle` | 副标题支持 `{1000点}` 这类高亮写法 |
| 一句话销售结论 | `intro` | 60 字以内 |
| 产品卖点 | `highlights` | 最多 3 条，每条 16–32 字 |
| 关键指标 | `metrics` 或 `metric_groups` | `metrics` 为 4 张单卡（值/标签/口径三行）；`metric_groups` 为 2 张大卡，每卡并排 2 组「值 + 标签」，适合把 4 个数字收成两句卖点 |
| 界面实拍截图 | `screens` | 2–4 张，每张配一句标签；**缺截图时海报会明显发空**。4 张图比例要一致（模板按 1.69 排版），新图比例不同时上下补白对齐，不要裁切，否则绿标签高度会参差 |
| 核心能力 | `sections` | 4 个，每个 3 条要点最佳 |
| 套餐内容表 | `package_table` | **一个套餐含多款物料时用它替代 `sections`**：3 列表格（组成 / 套餐物料 / 关键能力），组成列带 01·02 序号徽章，表尾写共用条款。每格 3 条要点、每条不超过 26 字，超了会顶穿海报底部 |
| 上市/退市与口径 | `source_note` | 放到底部 CTA 条，避免出现无来源数据 |
| 价格声明 | `price_note` | 小字、右对齐、贴在右下角，如「（价格为总代价，最终报价以内部报价系统为准）」；与 `source_note` 分工：前者是价格免责，后者是时间与口径 |
| 素材原文 | Word/PDF/彩页 | 可提供套餐说明、产品彩页，Skill 会自行提炼并可从彩页按坐标裁出高清界面图 |

> 套餐类海报：务必提供上市通知与套餐表原文。套餐里包含哪些物料、各自版本号、是否含运维服务，必须逐条对齐原文，不能把两款物料合并成一句话。内部材料里的接口人、内部系统路径、发文编号一律不进海报。

### 案例介绍推荐结构

直接提供以下信息即可，不需要先自己写成宣传稿：

```text
客户名称：
所属行业：
项目背景/业务场景：
遇到的问题：
采用的产品或方案：
具体实施方式：
产生的结果或能力变化：
可公开的范围：
不能公开的内容：
```

如果只提供一段自然语言，Skill 会负责拆分为：项目背景、挑战痛点、解决方案、案例价值，并把没有原文依据的内容列为营销改写，不擅自补造。

---

## 三、正式生成流程（三步门禁）

流程被拆成三个阶段，**每阶段结束都会停下来等你确认，没确认不会往下做**。这是刻意的：
海报的返工成本集中在"选错方向"，所以把选择前置。

```text
① 素材收集与核验  ──🚦门禁一──▶  ② 方案选版（强制等待选择）  ──🚦门禁二──▶  ③ 成品输出
   给清单 / 核验 / 标缺失            标题候选 × 字体 × 效果 × 版式          渲染 → 300DPI导出 → 质检
```

回退规则：改文案或换截图可以原地改；**改字体、改效果、改版式要回到第②步重新选**；换主题或换素材回到第①步。

### 第一步：素材收集与核验

**Skill 会先给你一张素材清单，而不是直接动手做海报。** 把素材放在同一个文件夹里，或在对话中逐项提供：

```text
case-materials/
├── customer-logo.png
├── company-logo.png
├── background.jpg
├── qr.png                  # 可选
└── case-introduction.docx  # 或 case-introduction.pdf / case-introduction.txt
```

然后逐项核验并回报结果：

- 客户名称与 Logo 是否对应；公司 Logo 是否清晰、是否需要裁掉透明留白
- 背景图片是否适合 1080×1440 竖版裁切
- 案例介绍中哪些是事实、哪些是营销表达
- 是否存在缺失素材或不能公开的信息（接口人、内部系统路径、发文编号等一律不进海报）

如果未提供标题，Skill 会提炼 2–5 个方向供选择。标题要求：一张海报一个主结论、两行以内、8–16 个汉字，
避免没有原文依据的“领先、第一、行业标杆”，可用 `{关键词}` 标记要分色的词。

### 🚦 门禁一（GATE 1）

Skill 会汇总「✅ 已收到 / ⚠️ 缺失 / 📌 营销改写」并停下。

- 必选项有缺 → 只输出缺失清单，**不会生成海报**
- 回复「按现有素材继续」= 明确放行，进入第二步

### 第二步：方案选版（必须等你选择）

Skill 会**同时**给出三类可编号的候选，让你看着图选：

**A. 大标题文案 3–5 条** —— 每条附一句话说明和营销改写标注。

**B. 字体 × 效果 × 配色** —— 出选版页与方案册：

- 9 款中文展示字体 × 11 种大标题效果，可交互切换
- 浅底专属效果：`block-tech`（斜切渐变块反白，清晰度最高）、`dual-tone`（双色分阶压印）、`outline-mix`（空心描边混排）
- 深底效果：`tech-shadow`（设计师版科技主题阴影）、`neon`
- 复杂中文字距保护、多笔画字不糊字

**C. 版式与背景方向** —— 产品版浅底（794×1123）还是案例版深底（1080×1440）；
指标区用 4 张单卡还是 2 张品牌色渐变大卡；是否放套餐内容表、是否放界面实拍。

每个选项都会写成「编号 + 渲染预览 + 一句话说明 + 可复制的 brief 字段」，例如：

```text
① 标题：AI 助手-{Q4冲刺套餐}
   字体：思源黑体 Heavy（sans）  效果：斜切渐变块反白（block-tech）
   brief 字段：title_font="sans", title_effect="block-tech"
```

### 🚦 门禁二（GATE 2）

**在你回复编号或明确组合之前，Skill 不会渲染成品海报。** 允许的回复形式：

- 「选 2」「选 D」——整组采纳
- 「标题用 1，字体用 04」——跨维度组合
- 「② 但标题改成 ……」——采纳 + 局部修改
- 「除了 ③ 都行，你定」——授权 Skill 自选并说明理由

收到修改意见后，Skill 会**重出选项并重新等你确认**，不会直接改成品。

### 第三步：成品输出

确认后才会渲染与导出：

```bash
python3 scripts/render_poster.py <brief.json> --output <poster.html>
python3 scripts/export_png.py <poster.html> --output <poster@300.png> --dpi 300
```

出图后按下列清单质检：

- 文案口径与原始材料逐字一致（如“AI 助手授权”不是“AI 桌面云授权”）
- 多笔画标题字（服、刺、套、基、座）一眼可辨，无糊字无重影
- 4 张界面截图比例一致、绿标签齐平
- 表格与卡片未溢出，底部 CTA 条完整（取底部像素确认不是白边）
- Logo 未变形、未被裁切；二维码可识别且有静区
- 上市/退市时间、价格声明等免责信息齐全

案例海报默认尺寸 1080×1440，包含客户与公司联合品牌区、大标题与关键词高亮、多层科技背景、
1–2 个方案价值卡、客户背景图、客户名称与案例摘要、二维码或占位。

默认交付：

```text
product-主题-poster.html      # 可编辑
product-主题-poster@300.png   # 2481×3509px，A4 @300 DPI，可送印
product-主题-brief.json       # 可复现
```

需要印刷时再加 3mm 出血重导。

### 回退规则

- 改文案 / 换截图 → 原地改完重出，不必重走选版
- **改字体 / 改效果 / 改版式 → 回到第二步重出选项再等确认**
- 换主题 / 换客户 / 换素材 → 回到第一步

## 四、直接可用的请求模板

```text
请生成客户案例海报。

客户名称：示例制造
客户 Logo：/绝对路径/customer-logo.png
公司 Logo：/绝对路径/company-logo.png
背景图片：/绝对路径/background.jpg
二维码：/绝对路径/qr.png（没有可省略）

案例介绍：
在研发设计、3D设计、供应链数据防护等场景，采用统一桌面、集中存储，异地研发远程登录，模型不落地，权限按项目隔离。对供应商提供受限协作空间，支持脱敏图纸在线核验和轻量化 3D 文件旋转、测量、比对，原始模型不外发，所有访问全流程水印和审计。

海报大标题：可选，不提供则自动总结
输出：先给我字体和大主题效果选版，确认后再生成 PNG + HTML
```

## 五、工具入口

生成标题选版页：

```bash
python3 art-type/scripts/make_select_page.py "标题{关键词}文案" --output <输出目录> --default-fx block-tech
```

生成方案选版册（多条标题 × 字体 × 效果）：

```bash
python3 scripts/make_options.py --output <输出目录> --theme light \
  --option "文案A|sans|block-tech" --option "文案B|pangmen|dual-tone"
```

反查参考海报字体：

```bash
python3 art-type/scripts/match_font.py <海报图片> --text "标题文案" --box L,T,R,B
```

渲染正式海报：

```bash
python3 scripts/render_poster.py <brief.json> --output <poster.html>
```

导出 PNG（默认 300 DPI，可直接送印）：

```bash
python3 scripts/export_png.py <poster.html> --output <poster@300.png> --dpi 300
```

- 海报的 CSS 像素按 96 DPI 书写：产品版 794×1123、案例版 1080×1440。
- `--dpi 300` 会自动按 3.125 倍设备像素比渲染 → 产品版 2481×3509px、案例版 3375×4500px，均为 A4 @300 DPI。
- `--dpi 96` 只用于快速预览（794×1123）。不要直接用系统截屏当交付图，那样只有 96 DPI，印刷会发虚。
- 自定义画布用 `--width/--height`；浏览器路径异常时用 `--chrome` 指定。

`brief.json` 中可配置（**这三个字段就是第二步里让你选的东西**）：

```json
{
  "title_font": "sans",
  "title_effect": "block-tech",
  "title": "AI 助手-{Q4冲刺套餐}"
}
```

- 产品宣传海报（浅底）用浅底效果：`block-tech` / `dual-tone` / `tech-shadow-light`
- 客户案例海报（深底）用深底效果：`tech-shadow` / `neon`

完整的三步门禁流程、门禁话术与质检清单见 [references/workflow.md](references/workflow.md)。

### 版式硬规则（避免返工）

- 左上角公司 Logo 必须先用 alpha 通道裁掉透明留白；带大留白的原图缩到 46px 高会显得极小。
- 浅底海报不要用 `tech-shadow`，改用 `tech-shadow-light`。
- 挤出阴影会吃字口：**浅底海报优先用 `block-tech`（色块反白）或 `dual-tone`（双色分阶）承载颜色，字体只是被反白/压印，清晰度最高**。用挤出阴影时，要 1:1 看截图确认"服、刺、套"这类字是否一眼可辨。
- 中间 4 个数字不要排成 4 张白卡（像表格），用 `metric_groups` 做 2 张品牌色渐变卡，并在卡左上角给一个 `cap` 小标签。
- 截图区标题不要和截图内部自带文字重复。
- `sections` 用 `points`（3 条要点）比用 `body`（一段话）更饱满。
- `highlights` 给 4 条填满 2×2 网格；只给 3 条会在第 4 格留空。
- 产品版纵向满版填充，底部由 CTA 条收口，不留大片空白。
- 文案口径要与原始材料逐字核对，例如授权对象是"AI 助手授权"而不是"AI 桌面云授权"。
